# Worker Cloudflare — contém credenciais, não vai pro Git
worker-completo-pronto.js
.wrangler/cache/wrangler-account.json
.wrangler/

# Equipe de marketing (projeto Claude interno, não vai pro deploy)
marketing/

# Dependências e config local — nunca vão pro deploy (GitHub Pages serviria tudo público)
node_modules/
.vscode/
